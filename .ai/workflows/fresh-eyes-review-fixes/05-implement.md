---
schema: sdlc/v1
type: implement-index
slug: fresh-eyes-review-fixes
status: complete
stage-number: 5
created-at: "2026-07-06T11:45:23Z"
updated-at: "2026-07-06T14:07:20Z"
slices-implemented: 4
slices-total: 4
metric-total-files-changed: 51
metric-total-lines-added: 1596
metric-total-lines-removed: 221
tags: [isometric-core, isometric-compose, isometric-android-view, depth-sorting, culling, interaction, drag, config-honesty, lifecycle, rendering, robolectric]
refs:
  index: 00-index.md
  plan-index: 04-plan.md
next-command: wf-verify
next-invocation: "/wf verify fresh-eyes-review-fixes docs-housekeeping"
---

# Implement Index

## Cross-Slice Integration Notes

- **depth-correctness (complete):** All five bug fixes landed in `isometric-core`. The corrected depth formula `x+y-z/sin(α)` is now in `Point.depth(angle)`, `Path.closerThan` step 1, and the `DepthSorter` pre-sort (via `path.depth(projectionAngle)`). The `docs-housekeeping` slice depends on this slice's output — it should consume the gate-approved formula `x+y-z/sin(α)` and the `NoDepthSorting = insertion order` contract.
- **interaction-api-honesty (complete):** Eight findings resolved: H1+A3 exact un-projection drag fix, M3 annotation honesty, A2 abstract projectionVersion, L5 tile dispatch tests, L6 rotationOrigin non-inheritance, M4 KDoc clarification. AC-4 (emulator smoke) pre-registered as deferral; AC-21 satisfied by depth-correctness slice. API dumps regenerated.
- **view-module (complete):** Five findings resolved in `isometric-android-view`: H2 dirty-flag lazy re-projection, A1 setter invalidation semantics documented, M5 Paint/Path reuse in renderer, L7 onTouchEvent return value, L8 StrokeStyle API. First test suite for the module: 16 Robolectric tests, all passing. API dump regenerated. Key deviation: `engine` field widened to `SceneProjector` (final class cannot be subclassed); `internal cachedScene` accessor enables scene-structure assertions under Robolectric's stub Canvas. AC-7b emulator smoke and AC-10b pixel-level fringe comparison deferred to verify stage.
- **docs-housekeeping (complete):** Ghost directories and tarball deleted; Compose catalog aligned in app/build.gradle.kts; KDoc corrected in Point.kt, IsometricEngine.kt, Vector.kt, RenderContext.kt; seven site `.mdx` files updated (axis directions, depth formula, NoDepthSorting contract, @Stable note, projectionVersion requirement, screen-pixels delta note, Pyramid solid note, TileGrid/projector hit-testing note); `docs/` mirrors regenerated by sync-docs.js. AC-27 build verification deferred to verify.

## Shared File Notes

- `isometric-core/src/main/kotlin/.../Point.kt` — touched by both completed slices (depth-correctness added `depth(angle)`; interaction-api-honesty updated `distanceToSegmentSquared` KDoc). No conflict.
- `isometric-core/src/test/.../IsometricEngineProjectionTest.kt` — touched by both (depth-correctness added test cases; interaction-api-honesty added `override val projectionVersion` to the anonymous fixture). No conflict.

## Recommended Next Stage

- **Option A (default):** `/wf verify fresh-eyes-review-fixes docs-housekeeping` — verify AC-24 through AC-27. AC-24 (docs match sync output) is already satisfied by the sync run in the implement pass. AC-25 and AC-26 are static checks. AC-27 needs `./gradlew :app:assembleDebug` and optionally an AVD boot.
- **Option B:** `/wf review fresh-eyes-review-fixes docs-housekeeping` — skip verify (all ACs are documentation/deletion checks; no testable behavior; no code written).
- **Option C:** `/wf verify fresh-eyes-review-fixes view-module` — complete the pending verify for the view-module slice if that is still outstanding.
